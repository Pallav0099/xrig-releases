# XRIG AI PC v1.0.1-rc.2 — Phase 5 clean-runner record

Status: **complete under the revised co-shipped factory scope**. This is a
candidate pre-release record; it does not promote `latest` or a stable
release.

## Immutable candidate

| Field | Value |
| --- | --- |
| Release tag | `v1.0.1-rc.2` |
| Release URL | `https://github.com/Pallav0099/xrig-releases/releases/tag/v1.0.1-rc.2` |
| Release workflow | `https://github.com/Pallav0099/xrig-releases/actions/runs/29378600661` |
| Manifest SHA-256 | `4dc15bdd1ab58ddeb458b4fdc4d05636f1b5b3c542c828a0a6a130655e9cb005` |
| Platform source SHA | `6586c293a4dbd6df56b3ce6cd87029fcf8013edf` (`v1.0.0`) |
| Vertex source SHA | `1944d550cbd854d1750d321679d7d45a3e80f183` (`v1.0.1-rc.2`) |
| Kai source SHA | `30b2f17fa1ef1bcf3e58e346d509a4f4619411b0` (`v1.0.1-rc.2`) |
| Manifest channel | `candidate` |

## Completed independent evidence

- Independent versioned-release verification passed: canonical manifest,
  all 12 declared artifacts, SHA-256/size checks, detached signatures, and
  altered-artifact/manifest rejection.
- Public Kai and Vertex bootstraps passed the missing-Platform gate with only
  the intended owner message and controlled exit `41`.
- The clean Windows VM passed both inactive-device gates with the intended
  owner message and controlled exit `42`.
- The clean Windows VM passed manifest verification, all 12 artifact checks,
  offline local Verify, and the online active-device check.

Raw evidence is retained outside source repositories at
`C:\XRIG\Acceptance\phase5\v1.0.1-rc.2`.

## Explicit deferrals

XRIG ships Kai and Vertex together on supported factory hardware. The following
standalone/non-certified-VM cases are deferred to the physical-hardware lane:

- Kai-only installation and Repair Vertex UI;
- Vertex-only/non-certified GPU configuration UX;
- Kai/Vertex installation-order permutations and standalone visual snapshots.

Phase 6 remains the release gate for a real certified-GPU factory installation,
Vertex health/model/inference, Kai-to-Vertex chat, rebooted offline operation,
and the complete owner-facing UI path.
